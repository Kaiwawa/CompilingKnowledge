So, I discovered new ways to manipulate a GitHub repo on shell.

If you did something bad (like .env on public repo, which isn't my case :D, this time), you can run `git rm -r --cached [what u want to remove]`. 
You can also edit your commit with `git commit -e`.